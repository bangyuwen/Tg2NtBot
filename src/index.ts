require('dotenv').config();

const {
  getDBContent,
  getPageID,
  createPage,
  retrievePage,
  retrieveBlock,
  retrieveBlockChild,
  appendBlock,
} = require('./notion');

// getDBContent();
// getPageID();
// createPage();
// retrievePage();
appendBlock();
// retrieveBlock();
// retrieveBlockChild();
